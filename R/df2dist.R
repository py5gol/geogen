#' A data frame to distance matrix function
#'
#' This function allows you to convert from a data frame
#' to a matrix of distance.
#' @param dat Data frame to be converted to distance matrix.
#' @keywords distance dataframe
#' @export
#' @examples
#' df2dist()

df2dist <- function(dat) {
  return (1)
}